package com.rem.backend.analytics.reporting.shared.enums;

/**
 * Hints to the frontend on how to format a value (card value, table cell, etc.).
 */
public enum ValueType {
    TEXT,
    NUMBER,
    CURRENCY,
    PERCENT,
    DATE,
    DATETIME,
    BADGE,
    BOOLEAN
}
