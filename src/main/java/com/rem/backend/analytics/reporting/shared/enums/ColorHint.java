package com.rem.backend.analytics.reporting.shared.enums;

/**
 * Semantic colour hint. The frontend maps these to its own palette/theme.
 * Intentionally semantic (not literal colours) so dashboards stay theme-able.
 */
public enum ColorHint {
    PRIMARY,
    SUCCESS,
    WARNING,
    DANGER,
    INFO,
    NEUTRAL
}
